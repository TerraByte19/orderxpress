# OrderXpress – Projektkontext für Claude

QR-Bestellsystem für ein Restaurant. Diese Datei ist die Übergabe an neue
Chat-Sitzungen: Stand, Entscheidungen, offene Punkte. Der User (Adham) ist
Einsteiger – Erklärungen einfach halten, auf Deutsch antworten.

## Fachlicher Ablauf (Kern der App)

1. Jeder Tisch hat einen QR-Code (Inhalt: `<public-base-url>/t/<qrToken>`, geheimer UUID-Token, NICHT die Tischnummer).
2. Gast scannt → `POST /api/guest/scan/{qrToken}` → Tisch-Sitzung mit Status PENDING.
3. Inhaber bekommt live (SSE) die Meldung **„Tisch Nr. X freigeben?"** und genehmigt/lehnt ab (Absicherung gegen Fremd-Scans).
4. Nach Freigabe (APPROVED): Gast sieht Menü, bestellt mit seinem `sessionToken`.
5. Bestellung geht live an die Küche (SSE) und wird als Bon gedruckt (ESC/POS).
6. Inhaber beendet die Sitzung (`close`) → Tisch wieder frei.

## Stack & wichtige Versionsentscheidungen

- **Spring Boot 4.1.0** (Parent-POM), **Java 17**, Maven. User baut lokal in PowerShell/IntelliJ; die Cowork-Sandbox hat KEINEN Zugriff auf Maven Central (nicht dort bauen, Versionen stattdessen per web_fetch auf repo.maven.apache.org verifizieren).
- Boot-4-Fallstricke (bereits berücksichtigt, bei neuem Code beachten):
  - `spring-boot-starter-web` ist deprecated → wir nutzen **`spring-boot-starter-webmvc`**.
  - `@AutoConfigureMockMvc` liegt jetzt in **`org.springframework.boot.webmvc.test.autoconfigure`** und braucht die Test-Dependency **`spring-boot-webmvc-test`**.
  - **Jackson 3**: KEINE direkten `com.fasterxml.jackson`-Imports verwenden (Records + jakarta-Validation reichen).
  - JUnit 6 (Pakete `org.junit.jupiter` unverändert), Hibernate 7, Security 7 (nur Lambda-DSL).
- H2 dateibasiert (`./data/`), `ddl-auto: update`. springdoc-openapi **3.0.3** (3.x = Boot-4-Linie), ZXing 3.5.4 für QR-PNGs.

## Architektur (Paket `com.orderxpress`)

- `domain`: RestaurantTable, TableSession (PENDING/APPROVED/REJECTED/EXPIRED/CLOSED), MenuCategory, MenuItem, CustomerOrder (NEW→IN_PREPARATION→READY→SERVED, CANCELLED; „Order" ist SQL-reserviert), OrderItem (Name/Preis-Schnappschuss).
- `service`: TableSessionService (Scan/Freigabe/Abklingzeit), OrderService (Preise NUR serverseitig, Statusübergangs-Map), MenuService, AdminCatalogService (CRUD), PrintService (@Async), QrCodeService, SseHub (SSE an Inhaber+Küche, Heartbeat 25s), DomainEventListener (**@TransactionalEventListener AFTER_COMMIT** – SSE/Druck erst nach DB-Commit).
- `service.printing`: ReceiptPrinter-Interface; LoggingReceiptPrinter (mode=log) / EscPosNetworkPrinter (mode=network, Port 9100, CP858, Tischnummer doppelt groß). Druckfehler bricht NIE die Bestellung ab → `printed`/`printError` an der Bestellung, Küche kann nachdrucken.
- `web`: GuestController (öffentlich), AdminController (Rolle OWNER), KitchenController (OWNER+KITCHEN), GlobalExceptionHandler (ProblemDetail, deutsche Meldungen).
- **Multi-Store (seit 12.07.2026):** Die App ist eine PLATTFORM mit mehreren Läden. Jeder `Restaurant`-Datensatz hat eigene Tische/Kategorien/Gerichte/Sitzungen/Bestellungen und Design. `RestaurantTable` und `MenuCategory` tragen `restaurant_id` (Unique jetzt PRO Laden: Tischnummer bzw. Kategoriename); `MenuItem` erbt den Laden über die Kategorie; Sitzungen/Bestellungen über den Tisch. Mandanten-Trennung läuft über `CurrentUser.restaurantId()` (aus dem `StoreUserDetails`-Principal) - Services filtern ALLE Queries darauf. SSE ist je Laden getrennt (SseHub-Maps nach restaurantId; DomainEvents tragen `restaurantId`).
- Security: Basic Auth, stateless, CSRF aus. **Plattform-Admin** aus `application.yml` (`orderxpress.security.platform-admin`, `{noop}`/`{bcrypt}`) - legt Läden an (`/api/platform/**`, Rolle PLATFORM_ADMIN, Seite `/platform.html`). **Inhaber/Küche** liegen in der DB (`app_users`, BCrypt) mit Laden-Zuordnung; `StoreUserDetailsService` lädt beide Quellen. Der Inhaber legt seine Küchen-Logins selbst an (`/api/admin/users`). Gast-Schutz über geheime Tokens + Freigabe. Spam-Schutz: 5 Min Abklingzeit nach Ablehnung.
- Swagger UI: `/swagger-ui.html` (Authorize-Button für Basic Auth). H2-Konsole: `/h2-console`. Beide permitAll → vor Produktion sperren.

## Konventionen

Bezeichner Englisch, Kommentare/Fehlermeldungen Deutsch (ASCII-Umschreibung ue/oe/ae in Java-Dateien). DTOs als Records, keine Entities nach außen. `open-in-view: false`, EntityGraph/join fetch gegen N+1.

## Status

**Fertig:** komplettes Backend, am 11.07.2026 vollständig grün getestet (test-api.ps1: 33/33 bestanden), Integrationstests (OrderFlow + Security), Code-Review, Swagger, PowerShell-Testscript `test-api.ps1` (im Projekt-Root, testet den ganzen Ablauf gegen die laufende App). Beispieldaten beim ersten Start (8 Tische, Speisekarte). **Frontend V1** (überholt seit dem Vite-Umbau, siehe „Neu (01.–03.08.2026)" unten – die Quelle liegt heute im Ordner `frontend/`, NICHT mehr unter `src/main/resources/static`: die HTML-Seiten direkt in `frontend/`, das gemeinsame `css/app.css` und die `js/*.js` unter `frontend/public/`): `guest.html` (+`js/guest.js`, Route `/t/{qrToken}` via PageController-Forward), `admin.html` (Login, Freigabe-Anfragen live, Tische mit QR-Overlay/Gast-Link/Sitzung-beenden, Bestellliste), `kitchen.html` (3-Spalten-Board, Statuswechsel, Storno, Nachdruck), gemeinsames `css/app.css` + `js/api.js`. Gelöste Stolperfalle: Browser-EventSource kann kein Basic Auth → eigener SSE-Client über fetch-Stream mit Authorization-Header (`OX.connectSse`), Login-Daten im sessionStorage. `TableDto` hat dafür `currentSessionId` bekommen.

**Neu (11.07.2026):** Speisekarten-Verwaltung KOMPLETT im Inhaber-Frontend (Kategorien + Gerichte anlegen/bearbeiten/löschen, Verfügbarkeits-Toggle) inkl. **Fotos**: Upload JPG/PNG max 5MB, wird serverseitig auf 1000px verkleinert + neu kodiert (MenuImageService, ImageIO), Ablage als BLOB in eigener Tabelle `menu_item_images` (Id = menu_item_id, damit Karten-Queries schlank bleiben), Auslieferung über `GET /api/guest/menu-items/{id}/image` (10 Min Cache), `imageUrl` in MenuItemDto/MenuItemAdminDto. Tisch-Verwaltung im Frontend (anlegen mit Auto-Nummer, bearbeiten, aktiv-Schalter, löschen). Neue Testklasse MenuImageIntegrationTest. BadRequestException + Handler (auch MaxUploadSizeExceeded → 400).

**Neu (11.07.2026, abends):** Detail-Ansicht pro Gericht auf der Gäste-Seite: Zeile antippen → Overlay mit großem Bild, Preis, Kurzbeschreibung und neuem Feld `details` (2000 Zeichen, „Zutaten & Details" für Inhalte/Allergene), plus Mengen-Stepper und Hinweis-Feld direkt im Overlay („+"-Button bleibt für Schnell-Hinzufügen, stopPropagation beachten). `details` wird im Admin über Textarea gepflegt (buildForm kann jetzt type=textarea; toggleAvailable MUSS details mitschicken, sonst Datenverlust). Beispieldaten enthalten Muster-Details mit Allergenen.

**Neu (12.07.2026): Umbau zur Multi-Store-Plattform + Design pro Laden.**
- **Domäne:** neue Entities `Restaurant` (Name/slug/active + Design: accentColor, backgroundColor, categoriesAsHamburger), `RestaurantAsset` (Logo/Hintergrund als BLOB, eigene Tabelle, unique restaurant_id+kind), `AppUser` (username/passwordHash/role OWNER|KITCHEN/restaurant/active), Enum `UserRole`, `AssetKind`. `RestaurantTable`/`MenuCategory` mit `restaurant_id` (+ Unique-Constraints pro Laden), Konstruktoren erweitert.
- **Security:** `application.yml` hat statt owner/kitchen nur noch `security.platform-admin`. `StoreUserDetails`/`StoreUserDetailsService`/`CurrentUser` unter `config.security`. SecurityConfig: nur PasswordEncoder-Bean + einzige UserDetailsService-Bean → Boot verdrahtet DaoAuthenticationProvider selbst (kein expliziter Provider-Bean, vermeidet Security-7-API-Fallen).
- **Services:** `PlatformService` (Läden CRUD + ersten Inhaber anlegen), `RestaurantAdminService` (Design, Logo/Hintergrund-Upload mit ImageIO-Verkleinern: Logo 600px, Hintergrund 1600px; Küchen-User-CRUD). Alle bestehenden Services (Menu/AdminCatalog/TableSession/Order/MenuImage) filtern über `CurrentUser.restaurantId()` bzw. beim Gast über qrToken/sessionToken → Laden. Fremde IDs werden bewusst wie „nicht gefunden" behandelt.
- **Controller:** `PlatformController` (`/api/platform/**`), AdminController um `/api/admin/design*` (+Logo/Background-Upload) und `/api/admin/users*` erweitert; GuestController: `/api/guest/menu/{restaurantId}`, `/api/guest/theme/{restaurantId}`, `/api/guest/restaurants/{id}/logo|background`. Scan-Antwort enthält jetzt `restaurantId`+`restaurantName`.
- **Frontend:** neue `platform.html`+`js/platform.js` (Läden anlegen). admin.html: Design-Karte (Farbwähler, Hamburger-Schalter, Logo/Hintergrund-Upload) + Küchen-Login-Karte. guest.js: lädt Theme nach Scan, setzt CSS-Variablen `--primary`/`--bg`, Logo, Hintergrundbild; Kategorien optional als Hamburger-Menü (cat-bar/cat-panel). app.css um Theming/Hamburger/Logo ergänzt.
- **Demo-Zugänge (DataInitializer, nur bei leerer DB):** Plattform-Admin `admin`/`admin123` (aus yml), Inhaber `inhaber`/`inhaber123`, Küche `kueche`/`kueche123`, Demo-Laden „Demo-Restaurant" (slug `demo`) mit 8 Tischen + Karte.
- **Tests angepasst:** Login-Passwörter auf DB-Werte (inhaber123/kueche123), Menü-Aufruf auf `/api/guest/menu/{restaurantId}` (restaurantId aus Scan-Antwort), test-yml auf `platform-admin`.

**WICHTIG - DB-Reset nötig:** Der Umbau fügt NOT-NULL-Spalten (`restaurant_id`) zu bestehenden Tabellen hinzu. Auf einer ALTEN Datenbank mit Daten scheitert `ddl-auto: update`. Vor dem ersten Start: App stoppen → Ordner `data/` löschen → starten (DataInitializer legt den Demo-Laden frisch an). Das ist ok, weil noch nichts produktiv läuft.

**Neu (12.07.2026, später): Rolle SERVICE/Kasse, Handy-Zugriff, Hintergrund-Fix.**
- **Rollen jetzt dreistufig:** `UserRole` = OWNER | SERVICE | KITCHEN. Neuer `ServiceController` (`/api/service/**`, Rollen OWNER+SERVICE): Freigabe-Anfragen, approve/reject/close, Tisch-/Bestellübersicht (nur lesen), SSE (gleicher Admin-Kanal). Gedacht für Kellner/Kasse, wenn der Inhaber nicht da ist. Küche unverändert. SecurityConfig-Regel `/api/service/**` ergänzt, `service.html` in Whitelist.
- **Mitarbeiter-Logins mit Rolle:** Inhaber legt Service- UND Küchen-Logins an (`/api/admin/users`, Rolle wählbar). `KitchenUserDto` → `StaffUserDto` (mit role), `CreateUserRequest` um `role` (Default KITCHEN, OWNER verboten). Repo: `findByRestaurantIdAndRoleInOrderByRoleAscUsernameAsc`. Frontend: admin.html-Karte „Mitarbeiter-Logins" mit Rollen-Select; neue `service.html`+`js/service.js` (mobil, große Buttons).
- **Handy-Zugriff:** `AdminCatalogService.baseUrl()` nimmt bei localhost-Konfig automatisch den Request-Host (z.B. http://192.168.x.x:8080) für QR-Codes/Gast-Links → QR funktioniert vom Handy im selben WLAN, ohne Konfiguration. Echte Domain in `public-base-url` wird bevorzugt. (Tomcat lauscht ohnehin auf allen Interfaces; ggf. Windows-Firewall für Port 8080 freigeben.)
- **Hintergrund-Fix:** `deleteAsset` lädt+löscht die Entity (statt derived delete) → Entfernen greift sofort. Gast/Admin cache-busten Logo/Hintergrund-URLs (`?v=timestamp`), damit ersetzte/entfernte Bilder nicht aus dem Browser-Cache hängen bleiben.
- **Demo-Zugänge erweitert:** zusätzlich `service`/`service123` (Rolle SERVICE) im Demo-Laden.

**Neu (13.07.2026): Personen pro Tisch (Guest), Beitritt, geteilte Rechnung, Kasse.**
- **Kern-Idee:** Mehrere Personen am selben QR/Tisch. Neue Entity `Guest` (session, guestToken, name, host, status PENDING/APPROVED/REJECTED) + Enum `GuestStatus`. Jeder Scan erzeugt eine eigene Person mit eigenem `guestToken`. Erste Person = **Gastgeber** (host), wird vom **Laden** freigegeben (Session-Approve gibt auch den Gastgeber frei). Jede weitere Person, die denselben Code scannt, ist ein Beitretender (PENDING) und wird vom **Gastgeber** freigegeben (nicht vom Laden).
- **Bestellungen pro Person:** `CustomerOrder` hat `guest_id`; `placeOrder` läuft jetzt über `guestToken` (nicht mehr sessionToken). `OrderItem` hat `paid`/`paidAt`. Bestellungen werden in der geteilten Rechnung nach Person gruppiert.
- **Geteilte Rechnung + Kasse:** `BillingService` baut die Rechnung (Positionen je Person, Summen offen/bezahlt, stornierte Bestellungen ausgenommen). Gast: `GET /api/guest/guests/{token}/bill` (nur ansehen + Positionen auswählen, Summe sehen). Kasse (OWNER+SERVICE): `GET /api/service/tables/{tableId}/bill` und `POST /api/service/settle {orderItemIds}` markiert ausgewählte Positionen als bezahlt (Split-Zahlung, Mandanten-geprüft).
- **Guest-Endpunkte (öffentlich):** `POST /scan/{qrToken}` (liefert guestToken, isHost, sessionStatus, guestStatus, guestName, tableNumber, restaurantId/-Name), `GET /guests/{token}` (Status-Polling), `PUT /guests/{token}/name`, `GET /guests/{token}/join-requests`, `POST /guests/{token}/join-requests/{joinerId}/approve|reject`, `GET /guests/{token}/orders`, `GET /guests/{token}/bill`. (Alte `/api/guest/sessions/{sessionToken}`-Endpunkte entfallen; TableSessionService.getStatus/SessionStatusResponse sind ungenutzt.)
- **Services/Repos:** neue `GuestService`, `BillingService`; `GuestRepository`, `OrderItemRepository`. `TableSessionService.scan` erzeugt Guests; `approve` gibt den Gastgeber mit frei, `reject` lehnt alle Personen ab.
- **Frontend:** guest.js komplett auf Guest-Modell: Gastgeber-/Beitritts-Warteschirme, Namensleiste (`PUT name`, Standard „Gast N" — Handy-Name ist technisch NICHT auslesbar), Beitritts-Banner (Gastgeber lässt per „Ja, reinlassen" frei), geteilte Rechnung mit Auswahl-Summe; guestToken wird pro QR in `localStorage` gemerkt (Neuladen = gleiche Person). service.html/js: Kasse-Karte (Tisch → Positionen wählen → Summe → „Als bezahlt markieren").
- **Tests (stark erweitert, ~76 @Test):** gemeinsame `IntegrationTestBase` (Helfer: eigenen Laden+Kategorie+Gericht+Tisch anlegen, Scan/Freigabe). Jeder Test legt seinen EIGENEN Laden mit eindeutigem slug an (keine gegenseitige Störung; die alte `findAll()`-Abhängigkeit auf Demo-Tische ist raus - OrderFlow/MenuImage nutzen jetzt eigene Läden). Klassen: Platform, MultiTenantIsolation, MenuCatalog, TableManagement, StaffUser (Rollen/Login/sperren/Passwort), DesignTheme (inkl. Logo/Hintergrund-Upload), GuestJoinBilling (Beitritt/Ablehnen/Rechnung/Kasse), KitchenOrderStatus (Statusmatrix/Storno/Nachdruck), Security (Rollen inkl. SERVICE/PLATFORM), OrderFlow, MenuImage. **Wichtig bei Geldbeträgen:** computed BigDecimal wie `0.00`/`7.00` werden als Double serialisiert → im Test `.value(7.0)` statt `.value(7)`; nur `BigDecimal.ZERO` kommt als `0` (Integer).
- **Manuelles Test-Tutorial:** `ANLEITUNG-TESTEN.md` im Projekt-Root (Schritt für Schritt durchs Frontend).

**WICHTIG - erneuter DB-Reset:** neue Tabelle `guests` + NOT-NULL `customer_orders.guest_id` + `order_items.paid`. Auf alter DB scheitert `ddl-auto: update`. Vor dem Start: App stoppen → `data/` löschen → starten.

**Neu (22.07.2026): Ein Login für alle Ansichten + Geräte-Anmeldung per QR (Idee aus dem Stempelkarte-Projekt).**
- **Problem vorher:** pro Laden vier Accounts (Plattform-Admin, Inhaber, Service, Küche) und jede Seite verlangte ein eigenes Login — weil der Auth-Header im `sessionStorage` lag (gilt nur pro Tab).
- **Ein Login:** Auth liegt jetzt im `localStorage` (tab-übergreifend). Neuer Endpunkt `GET /api/me` (Rolle + Laden) und `OX.buildNav(active)` baut oben eine Leiste *Inhaber · Service · Küche* — der Inhaber sieht alle drei, Service/Küche nur ihre eigene. Einmal anmelden reicht.
- **Geräte statt Mitarbeiter-Passwörter:** neue Entity `StaffDevice` (label, role SERVICE|KITCHEN, `deviceToken` dauerhaft, `activationToken` EINMALIG, activated/revoked, lastUsedAt). Inhaber legt ein Gerät an → QR-Code → Tablet scannt `/d/{activationToken}` → `POST /api/device/activate/{token}` tauscht den Einmal-Token gegen den Gerätetoken (Token wird dabei verbraucht). Danach schickt das Gerät bei jeder Anfrage den Header `X-Device-Token`.
- **`DeviceTokenFilter`** setzt denselben `StoreUserDetails`-Principal wie ein normaler Login → **alle bestehenden Services inkl. Mandanten-Trennung funktionieren unverändert**. Bewusst KEIN `@Component` (sonst doppelte Registrierung als Servlet-Filter), sondern in `SecurityConfig` per `addFilterBefore(..., BasicAuthenticationFilter.class)` eingehängt. `lastUsedAt` wird höchstens einmal pro Minute geschrieben.
- **Endpunkte:** `/api/admin/devices` (GET/POST/DELETE=sperren), `/api/admin/devices/{id}/regenerate` (neuer QR), `/api/admin/devices/{id}/qrcode` (PNG), `POST /api/device/activate/{token}` (öffentlich), `GET /api/me`.
- **Frontend:** `device.html`+`js/device.js` (Aktivierungsseite, leitet je nach Rolle auf service/kitchen weiter; **`js/device.js` gibt es seit dem Vite-Umbau nicht mehr** – die Seite ist als `frontend/src/pages/device.ts` neu geschrieben, siehe „Neu (01.–03.08.2026)" unten), Geräte-Karte in `admin.html` (anlegen, QR anzeigen, „zuletzt aktiv", neu einrichten, sperren), `.ox-nav` in app.css. Service/Küche überspringen den Login automatisch, wenn ein Gerätetoken vorhanden ist.
- **Sicherheits-Abwägung:** Wer das Tablet hat, ist drin (Token im localStorage). Deshalb Labels, „zuletzt aktiv" und gezieltes Sperren — und der QR-Code gilt nur einmal.
- **Tests:** neue `StaffDeviceIntegrationTest` (12 Tests): Einmal-QR, Rollengrenzen, Mandanten-Trennung, Sperren, `/api/me`, QR-PNG, OWNER-Gerät verboten.
- **Hinweis:** neue Tabelle `staff_devices` — reines Hinzufügen, `ddl-auto: update` schafft das ohne DB-Reset.

**Neu (23.07.2026): Render-Deployment vorbereitet.**
- **Docker:** `Dockerfile` (multi-stage: `maven:3.9.9-eclipse-temurin-21` baut → `eclipse-temurin:21-jre` läuft, `-DskipTests`, `MaxRAMPercentage=75` für 512-MB-Instanzen), `.dockerignore`, `render.yaml` (Blueprint, Web-Dienst Docker, `healthCheckPath: /`).
- **Konfig cloud-fähig:** `server.port: ${PORT:8080}`, `server.forward-headers-strategy: native` (HTTPS/QR hinter Render-Proxy korrekt). Datenbank per ENV überschreibbar (`SPRING_DATASOURCE_*`); PostgreSQL-Treiber im pom (runtime). Lokal bleibt H2-Default.
- **Sicherheit:** H2-Konsole + Swagger jetzt hinter Schalter `orderxpress.dev-tools` (Default true lokal). In SecurityConfig per `@Value` gelesen und die permitAll-Matcher NUR bei devTools=true VOR `anyRequest().denyAll()` eingehängt. Auf Render: `ORDERXPRESS_DEV_TOOLS=false`, `SPRING_H2_CONSOLE_ENABLED=false`, Plattform-Admin-Passwort per `ORDERXPRESS_SECURITY_PLATFORMADMIN_PASSWORD`.
- **Anleitung:** `RENDER-DEPLOY.md` (Stufe 1 H2-schnell, Stufe 2 PostgreSQL für bleibende Daten). Render deployt aus Git → Repo muss auf GitHub liegen.

**Neu (23.07.2026, Teil 2): Rollen geschärft – Inhaber-Statistik, Rolle WAITER, Name-Pflicht beim Gast, Küche pro Tisch.**
- **Statistik (nur Inhaber):** `StatsService` + `StatsDto`. Erste Version war eine Tages-Karte in admin.html; **seit 24.07.2026 eigene Seite** (siehe unten).
- **Neue Rolle WAITER (Kellner, nur lesen):** `UserRole` = OWNER | SERVICE | KITCHEN | **WAITER**. Kellner haben KEIN Passwort-Login – sie melden sich NUR per QR-Gerät an (StaffDevice mit Rolle WAITER) und sehen die Bestellungen pro Tisch mit Zuordnung (wer hat was bestellt). Neuer `WaiterController` (`/api/waiter/tables` → `BillingService.getActiveTables()`; `/api/waiter/events` → Admin-SSE-Kanal), Rollen OWNER+SERVICE+WAITER, rein lesend. `BillingService.getActiveTables()` liefert alle APPROVED-Tische als `BillDto`-Liste (nutzt die bestehende Session-Query). Security: `/api/waiter/**` + `/waiter.html` + WAITER in `/api/me`. Frontend: `waiter.html`+`js/waiter.js` (nur QR-Login, Tische aufklappbar, Positionen je Person). `device.js`: WAITER → `waiter.html`. **Bewusst KEIN Kellner-Umsatz** (Statistik bleibt pro Laden/Produkt).
- **Die Kasse verwaltet ALLE Geräte-QRs:** `ServiceController` um Geräte-Endpunkte erweitert (`/api/service/devices` GET/POST, `/devices/{id}/regenerate`, `/devices/{id}/qrcode` PNG, DELETE=sperren). `StaffDeviceService.createDevice` erlaubt WAITER/SERVICE/KITCHEN (nur OWNER verboten). `/api/service/devices` (GET) liefert ALLE Geräte des Ladens (jede Rolle). Frontend: Karte „Geräte & QR-Codes" in `service.html`/`service.js` zeigt/verwaltet ALLE QR-Codes (Küche, Kasse, Kellner) mit Rollen-Select beim Anlegen (QR im Overlay zeigen, neu einrichten, sperren). **Grund:** der Inhaber ist nicht immer im Laden, die Kasse schon → sie muss auch die vom Inhaber angelegten Küchen-/Kassen-QRs erneut anzeigen und neue anlegen können. (Der Inhaber-Zugang selbst ist kein Gerät und taucht dort nicht auf.)
- **Gast: Name-Pflicht vor dem Menü + Warenkorb übersteht Neuladen.** guest.js: nach Freigabe erscheint erst die Ansicht `view-name` – die Speisekarte kommt ERST, nachdem ein Name eingegeben und bestätigt wurde (`PUT /name`, Flag `ox-named-<guestToken>` in localStorage, einmal pro Person). Warenkorb wird als `ox-cart-<guestToken>` in localStorage gespeichert (id/quantity/note) und nach `loadMenu()` über `restoreCart()` wiederhergestellt – Reload verliert die Auswahl nicht mehr; bei Bestellung/Sitzungsende geleert. Reiner Frontend-Umbau, Backend-Default „Gast N" bleibt.
- **Küche pro Tisch gebündelt:** kitchen.js zeigt je Status-Spalte EINE Karte pro Tisch (alle Bestellungen desselben Tisches zusammengefasst, gleiche Positionen nach Name+Hinweis summiert). Buttons (Zubereiten/Fertig/Serviert/Storno/Bon) wirken auf ALLE gebündelten Bestellungen des Tisches. Reiner Frontend-Umbau (Status bleibt pro Bestellung).
- **Tests:** neue `StatsIntegrationTest` (Umsatz/Bestseller, Storno ausgenommen, Mandanten-Trennung, nur-Inhaber 403/401) und `WaiterViewIntegrationTest` (Kasse legt Kellner-QR an → Kellner sieht Tisch mit Person/Position, Rollengrenzen 403, `/api/me`=WAITER, Mandanten-Trennung, Owner darf auch). Bestehende `assertMoney`-Helfer genutzt.
- **DB:** keine Schema-Änderung außer dem WAITER-Enumwert (String-Spalte) – `ddl-auto: update` schafft das ohne Reset.

**Neu (24.07.2026): Statistik als eigene Seite (mehrere Zeiträume + Diagramme) + zwei Lösch-Wege.**
- **Eigene Seite `stats.html`+`js/stats.js`** (statt Karte in admin.html). In `OX.buildNav` neuer Punkt „Statistik" (nur OWNER); `/stats.html` in SecurityConfig-Whitelist. Die alte Statistik-Karte + `Admin.loadStats` sind aus admin.html/js raus.
- **Zeiträume:** `GET /api/admin/stats?range=today|week|month|total|custom` (+`from`/`to` als ISO-Datum bei custom). `StatsService.compute(range, from, to)`: Tagesbeginn/Woche(Montag)/Monatsanfang/Gesamt nach `ZoneId.systemDefault()`; `StatsDto` v2 = `{range, from, to, revenue, orderCount, itemCount, avgOrder, topProducts[], byDay[], byHour[]}` (byDay = Umsatz/Bestellungen pro Tag, byHour = 24 Stunden für Stoßzeiten). Frontend: Reiter + Datumsbereich, KPI-Kacheln (inkl. Ø-Bon), Top-Produkte-Balken, zwei senkrechte Balkendiagramme (CSS `.vchart/.vbar`, KEIN externes Chart-Lib). Repo: `findBy...CreatedAtBetween` ersetzt die alte GreaterThanEqual-Query.
- **Zwei Lösch-Wege (beide gewünscht):** (1) **Zurücksetzen** `POST /api/admin/stats/reset` setzt `Restaurant.statsResetAt = now` → Statistik zählt ab jetzt neu, **Bestellungen/Rechnungen bleiben** (compute nimmt `statsResetAt` als Untergrenze). (2) **Verlauf löschen** `DELETE /api/admin/stats/history` löscht Bestellungen NUR aus abgeschlossenen Sitzungen (CLOSED/REJECTED/EXPIRED) endgültig (`deleteAll` → OrderItems per cascade/orphanRemoval), laufende Tische bleiben; liefert `{deleted:n}`. Danger-Zone-Karte in stats.html mit doppelter Sicherheitsabfrage beim harten Löschen.
- **DB:** nur neue Spalte `restaurants.stats_reset_at` (nullable) – `ddl-auto: update` schafft das ohne Reset.
- **Tests:** `StatsIntegrationTest` erweitert (Zeiträume heute/woche/monat/gesamt + Vergangenheits-Datumsbereich=0, Ø-Bon, Reset behält Bestellungen aber zählt 0, Verlauf-Löschen entfernt nur abgeschlossene Sitzungen und lässt offene Tische stehen, Storno/Mandanten/Rollen wie zuvor). DTO-Felder in den alten Stats-Tests auf `revenue` umgestellt.

**Neu (24.07.2026, Teil 2): Kein Login-Flash mehr + installierbare App (PWA).**
- **Login-Flash weg:** Beim Umschalten zwischen Inhaber/Statistik/Service/Küche blitzte kurz die Login-Maske auf, weil `#view-login` sichtbar war, bis die Hintergrund-Prüfung `start()` aufrief. Jetzt: `#view-login` in admin/service/kitchen/stats.html standardmäßig `display:none`. Neuer Helfer `OX.ensureAuth(onReady, onLogin)` in api.js: ist eine Anmeldung vorhanden → sofort `start()` (App erscheint ohne Flash), Prüfung via `/api/me` läuft im Hintergrund; bei 401/403 `clearAuth()` + `location.reload()` → dann erst Login. Die vier `init()` nutzen das. (stats.js zeigt bei 403 die „nur Inhaber"-Meldung über den load()-Fehlerpfad.)
- **PWA (installierbar wie eine App, Wunsch „wie bei Stamp it", Handy/Tablet UND PC):** neue `manifest.webmanifest` (name/short_name, `display:standalone`, theme `#111827`, Icons), `service-worker.js` (network-first für Shell, `/api/**` bleibt Netz = immer frische Daten, offline-Fallback aus Cache; `install` per `allSettled` robust gegen fehlende Dateien), `js/pwa.js` (injiziert Manifest-Link + Apple/Theme-Meta in den `<head>` und registriert den SW). `pwa.js` ist nur auf den PERSONAL-Seiten eingebunden (index, admin, stats, service, kitchen, waiter, device, platform) - **NICHT auf `guest.html`**: Gäste sollen die App NICHT installieren, sie scannen nur den QR und bestellen im Browser. `guest.html`/`guest.js` daher auch nicht im SW-Shell-Cache. Icons unter `static/icons/` (192/512/maskable-512/apple-touch-180/favicon, blaues „OX", per PIL erzeugt). SecurityConfig-Whitelist um `/manifest.webmanifest`, `/service-worker.js`, `/icons/**` ergänzt (sonst blockt `denyAll`). Installation: Handy/Tablet „zum Startbildschirm hinzufügen"; PC Chrome/Edge → Installier-Symbol in der Adressleiste → eigenes Fenster. **HTTPS nötig** für echte Installation (lokal geht `localhost`); auf Render also automatisch gegeben.
- **DB/Backend:** keine Schema-Änderung; nur statische Dateien + eine Security-Zeile. Kein `data/`-Reset.

**Neu (25.07.2026): QR-Scan-Login (Kamera), Küchen-Bildschirm optional, sauberer Bon, Kellner darf abrechnen.**
- **Kellner darf abrechnen:** `WaiterController` um `POST /api/waiter/settle` erweitert (nutzt `BillingService.settle`, Mandanten-geprüft). Die Kellner-Tischliste enthält bereits alle `orderItemId`s, daher kein extra Bill-Abruf nötig. Frontend `waiter.js`: Tischliste bekommt „Kassieren" → eigene Abrechnen-Ansicht (`#view-kasse`) mit Auswahl + „Als bezahlt markieren"; Auto-Refresh pausiert im Kassieren-Modus. (Früheres „Kellner nur lesen" ist damit bewusst aufgeweicht - Kellner sehen UND kassieren, aber geben keine Tische frei.) Kein Kellner-Umsatz weiterhin.
- **Küchen-Bildschirm pro Laden ein/aus:** `Restaurant.kitchenDisplayEnabled` (Boolean, **nullable**, null gilt als true → kein DB-Reset). In `DesignRequest` (+`kitchenEnabledOrDefault()`), `RestaurantThemeDto` und `MeResponse` ergänzt; `updateDesign`/`buildTheme`/`MeController` setzen/liefern es. `OX.buildNav` blendet die Küchen-Ansicht aus, wenn `me.kitchenDisplayEnabled === false`. Toggle „Küchen-Bildschirm verwenden (aus = nur gedruckter Bon)" in der admin.html-Design-Karte (`design-kitchen`, load/save in admin.js). Der Bon-Druck läuft unabhängig davon weiter (DomainEventListener → PrintService), Läden ohne Küchen-Gerät arbeiten also nur mit dem Ausdruck.
- **Bon sauber:** `ReceiptData` um `restaurantName` + `guestName` erweitert (PrintService füllt sie). `ReceiptFormatter`: Kopf mit Ladenname (zentriert, GROSS), „NEUE BESTELLUNG", Tisch/Best.-Nr., Gastname, Zeit; lange Gericht-Namen/Hinweise werden am Wortende auf 42 Zeichen umgebrochen (Fortsetzung eingerückt). ESC/POS-Drucker druckt zusätzlich weiterhin „TISCH X" doppelt groß oben.
- **QR-Scan-Login mit Kamera:** `jsQR` selbst gehostet (`js/jsQR.js`), neuer `js/qrscan.js` (`QRScan.open()`): Kamera-Overlay, liest Geräte-QR (`.../d/<activationToken>`), ruft den bestehenden `POST /api/device/activate/{token}` auf, speichert den Gerätetoken und leitet je nach Rolle weiter (kitchen/waiter/service). Button „Mit QR-Code anmelden (Kamera)" auf `admin.html`, `service.html`, `kitchen.html` (unter dem Passwort-Login) und `waiter.html` (Scan als Hauptweg). CSS `#qr-overlay` in app.css. **Wichtig:** `getUserMedia` (Kamera) geht nur über HTTPS oder `localhost` - im LAN über `http://192.168…` bietet der Browser keine Kamera; Fallback-Hinweis per Toast, native Handy-Kamera als Alternative. Kein neuer Endpunkt/Token - reiner Frontend-Weg auf den vorhandenen Aktivierungs-Mechanismus.
- **Tests:** `WaiterViewIntegrationTest.kellnerKannAbrechnen` (Kellner sieht Position, settled, danach paid/offen=0), neue `KitchenDisplayIntegrationTest` (Schalter wirkt auf Design + /api/me, altes Design ohne Feld = Küche an).
- **DB:** nur neue nullable Spalte `restaurants.kitchen_display_enabled` - `ddl-auto: update` schafft das ohne Reset.

**Neu (26.07.2026): „Kellner rufen"-Funktion.**
- **Idee:** Ein freigegebener Gast drückt „Kellner rufen"; Kasse und Kellner sehen den Ruf live und haken ihn ab.
- **Domäne:** neue Entity `WaiterCall` (session, guest, status OPEN/DONE, createdAt) + Enum `CallStatus`; `WaiterCallRepository` (Dubletten-Check pro Sitzung, tenant-scoped Liste nur von APPROVED-Sitzungen, Mandanten-geprüftes Laden). Neue Tabelle `waiter_calls` – reines Hinzufügen, `ddl-auto: update` schafft das **ohne DB-Reset**.
- **Service/Event:** `WaiterCallService.call(guestToken)` (nur APPROVED-Gast/-Sitzung darf rufen; **pro Tisch höchstens EIN offener Ruf**; jedes Drücken meldet neu), `listOpen()`, `markDone(id)`. Neues `DomainEvents.WaiterCalled(restaurantId, tableNumber, guestName)` → `DomainEventListener.onWaiterCalled` → `sseHub.notifyAdmins(...,"waiter-called",...)` (nach Commit).
- **Endpunkte:** Gast `POST /api/guest/guests/{token}/call` (204); Personal `GET /api/calls` + `POST /api/calls/{id}/done` (neuer `CallController`). Security: `/api/calls/**` = OWNER/SERVICE/WAITER (Küche bewusst NICHT).
- **Frontend:** guest.js „Kellner rufen"-Knopf in der Namensleiste (30s-Sperre gegen Doppel-Rufe, erscheint nach Freigabe); service.js + waiter.js zeigen eine „Kellner-Rufe"-Karte (amber) mit „Erledigt" und poppen bei `waiter-called` einen Toast („Tisch X ruft"). Rufe aktualisieren sich über den bestehenden Admin-SSE-Kanal.
- **Tests:** `WaiterCallIntegrationTest` (Ruf erscheint bei der Kasse mit Tisch+Name, Dubletten-Schutz, erledigen leert die Liste, nicht-freigegebener Gast = 400, Mandanten-Trennung, Küche 403 / kein Login 401).

**Neu (01.–03.08.2026): Frontend-Umbau auf `frontend/` mit Vite + TypeScript.**
- **Neuer Ordner `frontend/`:** eigenständiges Vite+TypeScript-Projekt (siehe `frontend/package.json`, `frontend/vite.config.ts`), baut nach `src/main/resources/static`.
- **WICHTIG:** `src/main/resources/static` ist seitdem **generiert und gitignored** – dort darf NICHTS mehr von Hand bearbeitet werden. Vite baut mit `emptyOutDir: true`, jeder Bau leert den Ordner zuerst; handgeschriebene Änderungen dort gehen beim nächsten Frontend-Bau ersatzlos verloren.
- **Baukette unverändert nutzbar:** Das `frontend-maven-plugin` baut das Frontend in der Phase `generate-resources` mit (lädt Node selbst nach, kein Node auf dem Rechner oder im Docker-Image nötig) – `mvn spring-boot:run` bzw. `mvn clean package` genügen also weiterhin, ein eigener Schritt ist nicht nötig.
- **Für die Entwicklung zusätzlich:** `npm run dev` im Ordner `frontend` startet den Vite-Dev-Server (Port 5173) mit sofortigem Neuladen bei Änderungen; `/api/**` wird darin auf das parallel laufende Backend auf Port 8080 durchgereicht.
- **Stand der Umstellung (aktualisiert 05.09.2026):** Startseite (`index.html`), Geräte-Anmeldung (`device.html`) UND die **Gäste-Seite** (`guest`, Quelle unter `frontend/src/pages/guest/`, seit Commit `3aa28fa`) laufen auf dem neuen Design-System (TypeScript unter `frontend/src/`). Die restlichen sechs Seiten (`admin`, `kitchen`, `service`, `waiter`, `stats`, `platform`) laufen technisch weiterhin auf dem alten Stand (`.html` direkt unter `frontend/`, `css/app.css` + `js/*.js` unter `frontend/public/`, Vite reicht nur durch) - **ihre OPTIK ist seit „Neu (05.09.2026)" unten aber komplett auf dasselbe Design-System umgestellt** (gleiche Farben/Schriften/Radien, folgt sogar dynamisch dem Design des jeweiligen Ladens). Ein echter Umbau auf TypeScript-Komponenten (wie bei `guest`) steht weiterhin aus. Die App ist dabei jederzeit vollständig benutzbar.
- **Security:** `/assets/**` musste in `SecurityConfig` freigegeben werden, weil Vite gebündelte JS-/CSS-/Font-Dateien dorthin mit Prüfsummen-Hash im Dateinamen ablegt (sonst 401 statt 200) – Commit `b8525f4`, abgesichert durch `AssetsSecurityIntegrationTest`.

**Neu (28.08.2026): Bewegung + Laden-Stile für die Gäste-Seite.**
- **Vier neue Design-Achsen pro Laden**, Muster wie `kitchenDisplayEnabled` – vier **nullable** Spalten an `Restaurant` (`style_shape`, `display_font`, `cart_fly_style`, `order_confirm_style`), **kein DB-Reset**. Werte + Standard: `styleShape` SQUARE|SOFT (SQUARE), `displayFont` BRICOLAGE|FRAUNCES|SPACE_GROTESK|INSTRUMENT_SERIF (BRICOLAGE), `cartFlyStyle` PLUS|PHOTO (PLUS), `orderConfirmStyle` STAMP|CHECK (CHECK). In `DesignRequest` (`@Pattern` + `…OrDefault()`), `RestaurantThemeDto` (vier Felder am Ende) und `RestaurantAdminService.updateDesign`/`buildTheme`. `MeResponse` bewusst unangetastet. Tests: `GuestPageDesignIntegrationTest`.
- **Standard-Display-Schrift ist jetzt Bricolage Grotesque** (war Fraunces). Umschaltung nur über CSS-Variable `--ox-font-display` je `[data-font=…]` auf `<html>` (`tokens.css`); `data-shape="soft"` überschreibt die Radien. `theme.ts`/`laden-design.ts` setzen die Attribute; `leseModi(theme)` liefert Flieger-/Bestätigungs-Modus an `index.ts`. Neue `@fontsource`-Pakete: `bricolage-grotesque`, `space-grotesk`, `instrument-serif` (in `fonts.ts`). Fraunces `SOFT`/`WONK`-Feintuning liegt jetzt hinter `:root[data-font="fraunces"]`, sonst `font-optical-sizing: auto`.
- **14 Animationen auf der Gäste-Seite**, zentral: Keyframes + generische Klassen in `frontend/src/styles/animation.css`; gäste-seiten-spezifische Regeln in **`frontend/src/pages/guest/guest-motion.css`** (per `@import` aus `guest.css`, damit beide < ~250 Zeilen). Helfer in `frontend/src/pages/guest/animation.ts` (`staffelEin`, `fliegeZu`, `mitAnsichtsWechsel`, `bewegungAus`). Neues Modul `frontend/src/pages/guest/bon.ts` (Kassenbon-Warenkorb: Perforierung, Zeilen-Tippen, Stempel/Haken, Flug „zur Küche"). Effekte: Tischmarke atmet/schnappt, Warte-Puls, Karten-Stagger, gleitender Kategorie-Strich, „+"-Flieger + Zähler-Pop, Detail-Sheet slidet hoch, Bon-Zeilen tippen (nur beim ersten Öffnen), Status-Chip-Morph + blinkender Punkt, Ansichtswechsel per View-Transitions-API (Fallback synchron).
- **`prefers-reduced-motion`** schaltet alles ab: `base.css` nullt global alle Dauern, dazu explizite `@media`-Resets für „unsichtbar bis Animation"-Startzustände; die JS-Effekte (`fliegeZu`, `bestaetigeBestellung`) steigen bei `bewegungAus()` früh aus.
- **Admin:** vier `<select>` (`design-shape`/`-font`/`-fly`/`-confirm`) in der bestehenden Design-Karte der **alten** `admin.html` + `frontend/public/js/admin.js` (`loadDesign`/`saveDesign`), gespeichert über das vorhandene `PUT /api/admin/design`.
- Spec: `docs/superpowers/specs/2026-08-27-…-design.md`, Plan: `docs/superpowers/plans/2026-08-27-…`. Umgesetzt per Subagent-Driven Development (9 Tasks, Reviews je Task + Abschluss-Review).

**Neu (28.08.2026, Teil 2): Design-Vorlagen, mehr Schriften, Hell/Dunkel.**
- **Fünf Design-Vorlagen** (Bistro, Kasse, Nacht, Frisch, Klassik) — feste Kombis in `Admin.VORLAGEN` / `Admin.applyVorlage(name)` in `frontend/public/js/admin.js`, **nur Frontend, kein Server-Zustand**. Ein Klick auf einen Vorlagen-Knopf in der Design-Karte füllt Akzentfarbe, Hintergrundfarbe, Hell/Dunkel, Form, Schrift, Flieger, Bestätigung; danach normal „Speichern".
- **Drei zusätzliche Schriften** → 7 gesamt: `MANROPE`, `SORA`, `DM_SERIF`. `@fontsource`-Pakete (`@fontsource-variable/manrope`, `@fontsource-variable/sora`, `@fontsource/dm-serif-display`) in `fonts.ts`, `:root[data-font="manrope|sora|dmserif"]`-Blöcke in `tokens.css`, `SCHRIFT`-Map in `theme.ts`, `@Pattern` in `DesignRequest` erweitert.
- **Hell/Dunkel pro Laden:** neues **nullable** `Restaurant.darkMode` (Spalte `dark_mode`, `null` = hell, **kein DB-Reset**) — `isDarkMode()`, `DesignRequest.darkModeOrDefault()`, `RestaurantThemeDto.darkMode` (letztes Feld), `updateDesign`/`buildTheme`. Gäste-Seite: `LadenTheme.darkMode`, `laden-design.ts` reicht `dunkel: theme.darkMode` an `setzeLadenDesign` (setzt `data-theme="dark"`). Schalter „Dunkle Haut verwenden" in der Design-Karte. `theme.ts` leitet `--ox-text` weiter per Luminanz aus dem Hintergrund-Hex ab, die dunkle Haut tauscht zusätzlich Flächen/Rahmen/Signalfarben — darum setzen die Vorlagen (auch „Nacht": `#15181a`) immer einen expliziten Hintergrund-Hex.
- Spec: `docs/superpowers/specs/2026-08-28-design-vorlagen-und-mehr-schriften-design.md`, Plan: `docs/superpowers/plans/2026-08-28-…`. Umgesetzt inline (4 Tasks). Backend 127 Tests, Frontend 229 Tests grün.

**Neu (03.–05.09.2026): Kontakt/Galerie/Marken auf der Gäste-Seite + Personal-Ansichten komplett neu designt.**
- **Drei neue Design-Achsen am Laden** (Muster wie zuvor: nullable Spalten, kein DB-Reset): `openingHours`/`address`/`phone` - Footer auf der Gäste-Seite (`#menu-footer`, `laden-design.ts` `baueFooter`) zeigt sie, jede Zeile blendet sich einzeln aus wenn leer. Adresse bekommt einen „Route"-Link (Google-Maps-Suche, kein API-Key), Telefon einen Anruf- UND automatischen WhatsApp-Link (`wa.me`, aus den Ziffern der Nummer).
- **Bildergalerie** (Ambiente-/Stimmungsfotos, getrennt von Gericht-Fotos): neue Entity `RestaurantGalleryImage` + Repository, bis zu 8 Bilder pro Laden, Admin-Karte „Bildergalerie" (Upload/Entfernen), Foto-Streifen unter der Speisekarte (`#menu-gallery`). Endpunkte `/api/admin/gallery` (POST/GET/DELETE) + `/api/guest/restaurants/{id}/gallery` (Liste) + `/api/guest/restaurants/gallery/{imageId}` (Bild, öffentlich).
- **Gericht-Marken:** neues Enum `MenuItemBadge` (SCHARF|VEGETARISCH|VEGAN|BELIEBT|NEU), `@ElementCollection` an `MenuItem` (neue Tabelle `menu_item_badges`, reines Hinzufügen). Wählbar als Checkboxen im Speisekarten-Editor (`sheet.ts`), Pillen auf Gast-Karte + Detail-Overlay. **Wichtig:** jede Stelle, die ein Gericht per PUT speichert (altes `admin.js`-Formular, Editor-Reorder-Pfeile), muss `badges` unverändert mitschicken, sonst gehen gesetzte Marken verloren (gleiche Falle wie früher bei `details`).
- **Kategorien-Sprungnav sitzt jetzt IM Header** (`#topbar-kategorien` in `guest.html`), zentriert, statt als eigenes Element im Seiteninhalt - Grund: ein zweites sticky-Element im Inhalt geriet mit `document.startViewTransition()` durcheinander (siehe nächster Punkt). `.ox-topbar` ist dafür jetzt eine Spalte (`.ox-topbar__zeile` = alte einzige Zeile), betrifft auch `index.html`/`device.html`/`menu-editor.html` (mussten ihren Inhalt nur einwickeln, sehen unverändert aus).
- **Bug gefunden + gefixt:** `mitAnsichtsWechsel()` (`animation.ts`) nutzte die View-Transitions-API für einen weichen Ansichtswechsel - das macht `position: sticky` auf der Seite NACH dem ersten Wechsel dauerhaft kaputt (reproduziert: mit `startViewTransition` verschwand die sticky Leiste beim Scrollen, ohne blieb sie stehen). View-Transitions komplett entfernt, `wechsel()` läuft jetzt direkt.
- **Personal-Ansichten (`admin`/`kitchen`/`service`/`stats`/`waiter`/`platform`) komplett neu designt:** `frontend/public/css/app.css` ist eine volle Neufassung auf denselben Werten wie `tokens.css`/`components.css` (Bricolage Grotesque/Instrument Sans/IBM Plex Mono, warmes Papier-Weiß, Marken-Grün `#1f3d34` statt des alten Blau/Navy `#2563eb`/`#111827`) - selbe CSS-Selektoren wie vorher (`.card`, `button.green`, `.badge`, ...), also kein HTML/JS-Umbau nötig, reines Reskin. Google-Fonts-Links in den sechs Seiten ergänzt (liegen außerhalb der Vite-Pipeline, können die selbst gehosteten `@fontsource`-Pakete nicht nutzen).
- **Personal-Ansichten folgen jetzt sogar DYNAMISCH dem Design des jeweiligen Ladens** (nicht nur der festen Marken-Farbe): `OX.applyLadenTheme(theme)` in `api.js` holt sich `/api/guest/theme/{restaurantId}` (öffentlicher Endpunkt, jeder Mitarbeiter kennt seine `restaurantId` aus `/api/me`) und setzt `--primary`/`--primary-text` (Kontrast per WCAG-Formel, dieselbe Rechnung wie `theme.ts`), `--radius-sm`/`--radius`/`--radius-lg` (exakt dieselben Werte wie `tokens.css`: SQUARE=8/12/18px, SOFT=14/18/26px), lädt bei Bedarf die passende Google-Font nach und setzt `--font-display`, und schaltet `data-theme="dark"` auf `<html>` fürs Hell/Dunkel (`app.css` hat dafür einen `html[data-theme="dark"]`-Block bekommen). `buildNav()` ruft das automatisch auf; `waiter.js` (hat keine `buildNav`-Leiste) ruft es direkt in `init()`. `Admin.saveDesign()` wendet das frisch gespeicherte Theme SOFORT auf die eigene Seite an (PUT-Antwort enthält es bereits), kein Neuladen nötig.
- **Design-Karte in `admin.html` gegliedert:** war ein einziger Block aus ~20 Feldern, jetzt fünf benannte Abschnitte (Farben/Kategorien & Geräte/Stil/Vorhang-Intro/Kontakt & Social), reine `.mini-form__abschnitt`-Trenner, keine Felder verschoben. Reine An/Aus-Haken (Hamburger-Menü/Küchen-Bildschirm/Dunkle Haut/Verlauf) sind jetzt echte Schalter (`input.switch`, bleibt eine normale Checkbox darunter). Logo-/Hintergrund-Upload ist jetzt eine `.upload-card` (Vorschau + Datei-Knopf + Aktionen) statt einer rohen `<input type="file">`-Zeile; `::file-selector-button` bekommt überall den normalen Knopf-Look.
- **Vorhang-Willkommenstext:** Lesezeit (`HALT_MIT_MARKE_MS` in `vorhang.ts`) ist jetzt FEST 2000ms statt vom `introSpeed` skaliert (war vorher teils nur 330ms - viel zu kurz zum Lesen). Tempo betrifft nur noch die Vorhang-Bewegung selbst.
- **Konsistenz-Sweep über alle 8 Personal-/Übersichts-Seiten:** `index.html` fehlten Links zu `stats.html`/`menu-editor.html` (ergänzt); fehlende `autocomplete`-Attribute an mehreren Login-Formularen ergänzt; `kitchen.html` hatte einen veralteten Login-Hinweis („Zugangsdaten in application.yml" - stimmt seit den DB-Logins nicht mehr, `platform.html`s gleichlautender Hinweis ist dagegen weiterhin korrekt, Plattform-Admin bleibt yml-basiert); `menu-editor.html` zeigte durch das geteilte `wendeThemeAn()` fälschlich „– Bestellen" im Tab-Titel (neuer optionaler Parameter `titelZusatz`, Editor übergibt „Speisekarte bearbeiten"); Login-Fehlermeldung bei falscher Rolle (Status 403) fehlte in `admin.js`/`kitchen.js` (zeigten nur bei 401 „Falsche Zugangsdaten", jetzt wie überall sonst auch bei 403).
- **DB:** `restaurants.opening_hours`/`address`/`phone` (nullable) + neue Tabellen `restaurant_gallery_images`/`menu_item_badges` - alles reines Hinzufügen, `ddl-auto: update` schafft das ohne Reset.

**Neu (06.09.2026): Bild-Zuschnitt mit echter Gast-Vorschau.**
- **Vanilla-Widget `frontend/public/js/bildcropper.js`** (`OX.oeffneCropper(opts)`): Bild im Zuschnitt-Feld ziehen/zoomen, rechts ein `<iframe>` der echten Gaeste-Seite als Live-Vorschau. Formen kreis/quadrat/breit. Das Kandidatenbild geht per `postMessage` ins iframe; erst „Uebernehmen" laedt hoch. Klassisches Skript, damit `admin.js` (kein Modul) und der TS-Menue-Editor (`sheet.ts`) es gemeinsam nutzen.
- **Gast-Vorschau-Modus** (`frontend/src/pages/guest/vorschau.ts`): `guest.html?vorschau=1&restaurant=<id>&fokus=<ziel>` — kein Scan/Session, Bestellen/Warenkorb/Namensleiste/"Kellner rufen" aus (`:root[data-vorschau]` in `guest.css`). `message`-Handler (Origin-geprueft) tauscht Logo / Hero-Hintergrund / Gericht-Foto / Galerie gegen dataURL; `fokus` scrollt hin + hebt kurz hervor.
- **Einhang:** `admin.js` (Logo Kreis 600, Hintergrund breit 2.5 -> 1500, Galerie breit 1.5 -> 1200); `menu-editor/sheet.ts` (Gericht-Foto quadrat 900). Nutzt vorhandene oeffentliche Endpunkte (`/api/guest/theme|menu|restaurants/{id}/gallery`).
- **Frontend-Tests 293 gruen, Build sauber.** Kein Backend-Umbau. Spec + Plan unter `docs/superpowers/2026-09-06-bild-zuschnitt-mit-gast-vorschau`.

**Offen / nächste Schritte:**
1. Vor echtem Einsatz: Passwörter ändern, H2-Konsole + Swagger sperren, HTTPS, `public-base-url` setzen.
2. Später: PostgreSQL + Flyway (statt `ddl-auto: update`), echten Bondrucker testen (`printer.mode: network` + IP), evtl. Bezahlung.

**Betriebs-Regeln (wichtig, Ursache früherer Probleme):** Es darf nur EINE App-Instanz laufen (Port 8080, `netstat -ano | findstr :8080` prüfen). `data/` NIEMALS löschen, solange eine Instanz läuft – sonst entsteht eine halb kaputte Datenbank. Reset-Reihenfolge: App stoppen → `data/` löschen → starten.

**Lektionen aus der Fehlersuche (nicht wiederholen!):**
1. Dateien, die Claude über den Cowork-Mount schreibt, können ÄLTERE Zeitstempel haben als der letzte Maven-Build → Maven kompiliert sie dann NICHT („Nothing to compile"). Nach Claude-Änderungen immer `mvn clean test` / `mvn clean spring-boot:run`, oder Claude muss die Zeitstempel per `touch` anheben.
2. Windows PowerShell 5.1 hat zwei JSON-Fallen, die wie Backend-Bugs aussehen: (a) `Invoke-RestMethod` liefert JSON-Arrays als „Liste in der Liste“, (b) bei GENAU einem Element fehlt `.Count`. In `test-api.ps1` daher Listen IMMER via `Get-JsonList` holen und beim Zuweisen in `@(...)` wickeln. Diese beiden Fallen haben eine lange Phantom-Fehlersuche verursacht – der Server-Code (`findBySessionIdOrderByCreatedAtDesc` etc.) war nie kaputt.

Details/Beispiel-curls: siehe README.md.
